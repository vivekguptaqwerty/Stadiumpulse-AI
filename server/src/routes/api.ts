// Placeholder for future Express / Fastify server routing mapping endpoints
export const registerRoutes = (app: any) => {
  console.log("Registering routes for /api/zones, /api/gates, /api/sim, /api/fan/ask");
};
